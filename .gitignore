node_modules/
dist/
.code-viewer/
docs/superpowers/
*.tgz
.DS_Store
.env
.env.*

# data/test/<service>/.git は preview server を独立 cwd で動かすための内部 git。
# 外側のリポジトリには含めない (sub-repo として誤検出されるのを防ぐ)。
data/test/*/.git/
